//
//  ServiceListViewModel.swift
//  NTK
//
//  Created by Joey Chau on 09/03/2022.
//

import Foundation
import SwiftUI

class ServiceListViewModel: ObservableObject {
    
   /* func getSubList(rdfList: [Graph], graph: Graph) -> [Graph]{
        var list: [Graph] = []
        switch graph.narrower {
        case .stringArray(let strings):
            for string in strings {
                for service in rdfList {
                    if service.id == string {
                        list.append(service)
                        break
                    }
                }
            }
        default: print("No case")
        }
        return list
    } */
}
