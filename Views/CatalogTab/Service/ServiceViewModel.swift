//
//  ServiceViewModel.swift
//  NTK
//
//  Created by Torgeir Eikeland on 02/03/2022.
//

import Foundation
import SwiftUI


class ServiceViewModel: ObservableObject {
    /*
    @Published var apiData: [Tjenestebeskrivelse] = []{
        didSet{
            if apiData.isEmpty {
            
            } else {
                tjeneste = apiData[1]
                print(tjeneste)
            }
        }
    }
    @Published var tjeneste: Tjenestebeskrivelse?{
        didSet{
            print(tjeneste?.beskrivelse)
        }
    }
    @Published var serviceData: [Service] = []
    @Published var documentData: [DataRef] = []
    
    init(){
        serviceData = categories.demokratiOgInnbyggerrettigheter[0].subCategory[0].serviceList[0].service
        documentData = categories.demokratiOgInnbyggerrettigheter[0].subCategory[0].serviceList[0].service[0].dokumentRefSet
        
        apiModel.apiStuff(callback: { [weak self] responseList in 
            if let responseList = responseList {
                self?.apiData = responseList
            }
        })
    }
    
    func getSubTitle(){
        
    }
    */
}

